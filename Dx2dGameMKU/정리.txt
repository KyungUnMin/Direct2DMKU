팩토리
	다렉에서는 오브젝트를 얻어오기 위해서는 항상 팩토리를 통해서 얻을 수 있게끔
	만들어져 있다

	다렉 렌더링 구조는 
	non 블로킹 구조
		

		렌더 타겟 == hDC
		텍스처 뭐시기 == 비트맵

		스왑체인엣 ㅓ텍스처 만들어지고
			그 안에서 렌더타켓을 만들수 있음
				거기에 이미지를 그려야 화면에 나온다

-----------------------

프리컴파일
미리 빌드시켜놓고
이후엔 빌트X

C++ 미리 컴파일 -> 사용
헤더파일 이름 PrecompileHeader.h로 변경

각자의 프로그램에서 PrecompileHeader만들어주어야 함
모든 CPP위에 PrecompileHeader.h추가해야함

PrecompileHeader.h
CPP는 옵션에서 미리컴파일된 헤더 ->미리컴파일된 헤더 -> 만들기로 변경

렌더러 크기

---
<렌더링 파이프라인>
	인풋 어셈블러
		점 줁비, 근데 점 안에 위치와 색상 등이 함께 들어가 있어야 함
		버텍스 버퍼 -> 점
		인스턴스 버퍼 -> 점을 그리는 순서(혹은 인덱스 버퍼)
			보통 통상적으로 이 둘을 합쳐서 메쉬라고 부르기로 함
			하지만 Direct에서는 버텍스 버퍼만 메쉬라고 하고 이를 그리는 방법을 인스턴스 버퍼라고 한다

		CPU에 있던 버텍스 버퍼 정보를 GPU에 넣으므로써 버텍스 버퍼를 만드낟,
		GPU는 자료형에 관심없이 데이터 크기 등의 정보만 알면 된다

	 ID3D11Byffer 







